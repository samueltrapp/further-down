// import {useContext, useEffect, useState} from "react";
// import {RewardContext, RewardStateContext} from "../../contexts/RewardContext.tsx";
// import {CharacterContext} from "../../contexts/CharacterContext.tsx";
// import {PlayerType} from "../../../types/individual/characters.ts";

// const pickCharsPerPlayer = (playerCount: number) => {
//   switch (playerCount) {
//     case 1:
//       return 3;
//     case 2:
//       return 2;
//     case 3:
//     case 4:
//     default:
//       return 1;
//   }
// };

export function Rewards() {
  // const [hasName, setHasName] = useState(true);
  // const [userCharacters, setUserCharacters] = useState<PlayerType[]>([]);
  // const characters = useContext(CharacterContext);
  // const pending = useContext(RewardContext);
  // const pendingState = useContext(RewardStateContext);

  return <></>;
}
